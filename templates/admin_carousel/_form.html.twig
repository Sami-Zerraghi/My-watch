{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
<div class="d-flex flex-wrap justify-content-between mb-3">
    <div class="col-12 col-md-2  d-flex align-items-center">{{form_row(form.isActive)}}</div>
    <div class="col-12 col-md-3">{{form_row(form.rankNumber)}}</div>
    <div class="col-12 col-md-7 text-end">{{form_row(form.tag)}}</div>
</div>
    {{form_row(form.titre)}}
    {{form_row(form.texte)}}
    {{form_row(form.imageFile)}}
    <button class="btn btn-success">{{ button_label|default('Sauvgarder') }}</button>
{{ form_end(form) }}
